#pragma once

// Pre-1.0 Arduino header name. Adafruit_GFX.h falls back to this when the
// ARDUINO version macro is absent (as it is on the host build) — give it
// the same shim surface either way.
#include "Arduino.h"
